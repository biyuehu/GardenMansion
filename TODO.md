# Todo

## Backend

- [ ] Implement hight-level logger
- [ ] `User` model add `nickname` field which is changeable by user
- [ ] Remove `alive` and `admin` fileds of `User` model, add `level` field (base on aglebra data type, it seems to need update `type-generator` script)
- [ ] handler insreting new data need find max id and add 1 to it, then insert new data with new id
- [ ] Forbid not active user to login or use
- [ ] `when` replace all `if-else-then` in handlers logic
